openapi: 3.0.3
info:
  title: External Ref API
  version: 1.0.0
paths:
  /widgets/{widgetId}:
    get:
      $ref: ./operations/widgets.get.yaml
components:
  schemas:
    ErrorResponse:
      type: object
      additionalProperties: false
      required:
        - message
      properties:
        message:
          type: string
