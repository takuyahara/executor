---
"executor": patch
---

Fix embedded web asset serving in compiled binaries by serving Bun embedded files directly, and extend the release smoke test to verify root HTML plus JS/CSS asset responses.
